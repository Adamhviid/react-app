import React from 'react'
import PropTypes from 'prop-types'

const UseLike = props => {
  const {
    item,
  } = props
  return (
    <div>
    </div>
  )
}

UseLike.propTypes = {
  item: PropTypes.object
}

export default UseLike